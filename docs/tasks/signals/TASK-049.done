TASK-049 done — M3 replay gate: examples/go-timed/replay_test.go (//go:build integration) produces a deterministic timed-demo log via rtid, replays it through rtid -mode=replay-from-log into a fresh CapturingSink, and asserts the captured stream is byte-identical (header included) to the source. The existing eventlog.Replayer dispatches TimeRequested/TimeGranted/Halted via passthrough; no replayer extension was needed. The unskipped rti/spec/M3/{replay,determinism}_test.go scaffolds drive the same harness at the package boundary.
